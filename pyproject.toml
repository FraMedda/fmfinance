[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "fmfinance"
version = "0.0.3"
description = "Financial data utilities for Fama-French and FRED by Francesco M."
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
  {name = "Francesco Medda"}
]
keywords = ["fama-french", "fred", "finance", "economics"]
classifiers = [
  "Programming Language :: Python :: 3",
  "License :: OSI Approved :: MIT License",
  "Topic :: Office/Business :: Financial",
]
dependencies = [
    "pandas>=2.0",
    "requests>=2.28",
]

[project.urls]
Homepage = "https://github.com/Fra-Me-uniCA/fmfinance"

[tool.setuptools]
packages = ["fmfinance"]